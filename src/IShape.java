


public interface IShape {
    //declared using interface keyword
    //all methods are abstract: they carry the method signature but no implementation
    //all methods are automatically public

    double area();
    // argument types can be specified
    //example: double area(double length, double breadth );
    double perimeter();


}
